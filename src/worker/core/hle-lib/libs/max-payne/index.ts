/** Max Payne 1 exact-build inner-loop HLE registration. */
import { libRegistry } from '../../lib-registry';
import { maxPayneDescriptor } from './tree-find';

libRegistry.register(maxPayneDescriptor);
